import { Text, View, StyleSheet } from "react-native";

const NewMatchScreen = () => {
  return (
    <View style={styles.container}>
      <Text>NewMatchScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { flex: 1 }
});

export default NewMatchScreen;
